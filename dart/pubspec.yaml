name: chatproto
description: protobuf for chat
version: 0.0.1

environment:
  sdk: ">=3.3.1 <4.0.0"

dependencies:
  protoc_plugin: ^25.0.0
  grpc: ^5.1.0
  protobuf: ^6.0.0
  fixnum: ^1.1.1

dev_dependencies:
  lints: ^6.1.0
  test: ^1.25.15
